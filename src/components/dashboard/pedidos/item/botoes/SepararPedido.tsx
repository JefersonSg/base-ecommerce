import React from 'react';
import styles from './SepararPedido.module.css';

const SepararPedido = () => {
  return <button className={styles.separado}>Separar Pedido</button>;
};

export default SepararPedido;
