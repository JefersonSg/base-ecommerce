import React from 'react';
import styles from './EnviarPedido.module.css';

const EnviarPedido = () => {
  return <button className={styles.enviado}>Enviar Pedido</button>;
};

export default EnviarPedido;
