import React from 'react';
import styles from './CancelarPedido.module.css';

const CancelarPedido = () => {
  return <button className={styles.cancelar}>Cancelar Pedido</button>;
};

export default CancelarPedido;
