import React from 'react';
import styles from './ConfirmarPedido.module.css';

const ConfirmarPedido = () => {
  return <button className={styles.confirmar}>Concluir Pedido</button>;
};

export default ConfirmarPedido;
